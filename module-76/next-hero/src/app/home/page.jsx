import HomePage from '@/component/HomePage';
import React from 'react';

const page = () => {
    return (
        <div>
            <HomePage></HomePage>
        </div>
    );
};

export default page;